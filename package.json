{
  "name": "veloura",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://reetuTheCoder.github.io/veloura",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && touch out/.nojekyll && gh-pages -d out --dotfiles --no-history -b gh-pages"
  },
  "dependencies": {
    "next": "15.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
